{
  "name": "my-loom-app",
  "version": "1.0.50",
  "scripts": {
    "check": "bun loom doctor --strict && bun test",
    "test": "bun test",
    "dev": "bun run --watch src/index.ts",
    "loom": "bun run scripts/loom.ts",
    "hooks:install": "git config core.hooksPath .githooks",
    "prepare": "bun run hooks:install"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
